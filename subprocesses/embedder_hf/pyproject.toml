[project]
name = "embedder_hf"
version = "0.1.0"
dependencies = [
    "sentence-transformers>=3.0.0",
    "numpy>=2.0.0",
    "torch>=2.10.0",
]


[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
