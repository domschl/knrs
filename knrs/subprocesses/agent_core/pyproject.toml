[project]
name = "agent_core"
version = "0.1.0"
description = "Shared core library for agent subprocess backends"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
