[project]
name = "embedder_llama"
version = "0.1.0"
dependencies = [
    "llama-cpp-python>=0.3.0",
    "numpy>=2.0.0",
    "huggingface-hub",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
